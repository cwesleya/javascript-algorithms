
/**
  A palindrome is a text that is spelled the same way forward and backward, e.g., `abcba`.
  Write a function that returns `true` if a given text is a palindrome and `false` otherwise.

  * @param {string} str
  * @return {boolean}
*/

function isPalindrome(str) {

}

//module.exports = isPalindrome;
