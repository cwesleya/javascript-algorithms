/**
Write a function that takes an array of numbers and a target number, and
  returns the indices of the two numbers that when added together equal the target.

 * @param {number[]} array
 * @param {number} target
 * @return {number[]}
*/

function findTwoSum(array, target) {

}

//module.exports = findTwoSum;
