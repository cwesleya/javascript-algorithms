/**
Write a function that takes an array of numbers and returns the pivot index if it exists and `-1` otherwise.
The pivot index is the index where the sum of all the numbers left of the index equals the sum of all
 the numbers to the right of the index. You can assume the array will always contain at least 3 numbers.

 * @param {number[]} array
 * @return {number}
 */

function findPivotIndex(array) {

}

//module.exports = findPivotIndex;

