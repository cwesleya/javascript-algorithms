/**
Write a function that takes two strings and returns true if the first is a
  subsequence of the second, and false otherwise. A string `S` is a subsequence
  of another string `T` if it's possible to form `S` by simply removing characters from `T`.

 * @param {string} str1
 * @param {string} str2
 * @return {boolean}
*/

function isSubsequence(str1, str2) {

}

//module.exports = isSubsequence;

